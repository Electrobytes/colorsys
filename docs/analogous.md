# Analogous
Returns two colors adjacent to the input color. The spacing can be adjusted using the magnitude flag (-m) (default: 42).
## Usage
```
colorsys analogous "<color value>" [-m <spacing>] -i <input color model> -o <output color model>
```