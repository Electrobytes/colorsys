# Split Complementary
Returns the color adjacent to the opposite of the input color. To simply, analogous function is done after the complementary function. The spacing between the output and the complementary can be adjusted with the magnitude flag (-m) (default: 42).
## Usage
```
colorsys complementary "<color value>" [-m <spacing>]-i <input color value> -o <output color value>
```